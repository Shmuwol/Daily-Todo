<h1>All Tasks</h1>

<ul>
  <% @tasks.each.with_index(1) do |task, index| %>
    <li>
      (<%= index %>)
      <%= task.name %>
      <small>[<%= task.list.title %>]</small>
    </li>
  <% end %>
</ul>
